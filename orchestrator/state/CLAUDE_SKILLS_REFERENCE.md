# Claude Skills Reference (from Desktop/Claude_Codes (1).pdf)

Token-optimal shortlist for orchestrator use:

- QA / Risk / Checklist tasks:
  - `/audit` `/redteam` `/premortem` `SENTINEL` `/digest`
- Plan / Launch / Strategy tasks:
  - `PARETO` `/scenario` `BLINDSPOT` `/trim`
- Architecture / Critical reasoning tasks:
  - `ARCHITECT` `XRAY` `/blindspots` `CHAINLOGIC`

Usage rule in orchestrator:
- Only for Claude tasks.
- Added as short hint line: `CLAUDE_HINTS(use-if-helpful): ...`
- Max 4 hints to keep token usage low.
